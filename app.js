const invitation=document.getElementById('invitation');const toast=document.getElementById('toast');const dialog=document.getElementById('rsvpDialog');const form=document.getElementById('rsvpForm');let toastTimer;function showToast(message){clearTimeout(toastTimer);toast.textContent=message;toast.classList.add('show');toastTimer=setTimeout(()=>toast.classList.remove('show'),2400)}async function loadArtwork(){try{const res=await fetch('assets/wedding-reference.b64',{cache:'force-cache'});if(!res.ok)throw new Error('asset fetch failed');const b64=(await res.text()).trim();invitation.src=`data:image/webp;base64,${b64}`}catch(e){showToast('Taklifnoma rasmi yuklanmadi.')}}function scrollToImageY(y){const wrap=document.querySelector('.artboard-wrap');const scale=wrap.clientWidth/1024;window.scrollTo({top:y*scale-10,behavior:'smooth'})}document.querySelectorAll('[data-target]').forEach(btn=>btn.addEventListener('click',()=>scrollToImageY(Number(btn.dataset.target))));document.querySelectorAll('[data-open-rsvp]').forEach(btn=>btn.addEventListener('click',()=>dialog.showModal()));document.querySelector('[data-close-rsvp]').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});document.querySelector('[data-map]').addEventListener('click',()=>window.open('https://yandex.uz/maps/?text=Sherbudin%20Buxoro','_blank','noopener,noreferrer'));document.querySelector('[data-music-toggle]').addEventListener('click',()=>{const audio=document.getElementById('weddingAudio');if(!audio.src){showToast('Musiqa faylini assets/music.mp3 ga qo\'ysangiz avtomatik ishlaydi.');return}if(audio.paused){audio.play().then(()=>showToast('Musiqa yoqildi ♫')).catch(()=>showToast('Brauzer musiqa ijrosini blokladi.'))}else{audio.pause();showToast('Musiqa o\'chirildi')}});form.addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(form).entries());localStorage.setItem('wedding-rsvp',JSON.stringify(data));dialog.close();form.reset();showToast('Rahmat! Tashrifingiz qayd etildi ♡')});loadArtwork();